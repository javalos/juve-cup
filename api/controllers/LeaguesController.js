/**
 * LeaguesController
 *
 * @description :: Server-side logic for managing Leagues
 * @help        :: See http://links.sailsjs.org/docs/controllers
 */

module.exports = {

  addTeam: function (req, res) {
    //The 18th scudeto was won in 1978. 37 years later, Milan and Inter still have 18.

    
    //Use this to find one model instance:
    //ModelName.findOne(modelId)
    //  .then(function (modelResult) {
    //  
    //  });

    //Use propagate function for nested attributes

    //Use modelResult.save() to persist changes

    res.end();
  }

};

